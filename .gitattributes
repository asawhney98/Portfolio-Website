git add index.html "Portfolio Website_CLAUDE.md" && git commit -m "Add horizontal career timeline to hero section" && git push origin main# Auto detect text files and perform LF normalization
* text=auto
